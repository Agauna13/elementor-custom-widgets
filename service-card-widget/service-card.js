// AG Custom Widget Architecture
// Automatically loaded by widgets-loader.php

jQuery(document).ready(function ($) {
    // Add custom widget scripts here.
    // Make sure to bind to Elementor's init hook for live preview support.
    $(window).on('elementor/frontend/init', function () {
        /*
        elementorFrontend.hooks.addAction( 'frontend/element_ready/service_card.default', function($scope) {
            // Init code
        } );
        */
    });
});
